#include <iostream>

int main() {
    std::cout << "Hello, World from router service!" << std::endl;
    std::cin.get(); // Wait for user input before exiting
    // This is to ensure the console window stays open until the user presses Enter.
    return 0;
}